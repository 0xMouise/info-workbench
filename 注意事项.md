# 信息收集工作台 v9｜注意事项

## 1. 只在明确授权范围内使用

本项目用于信息整理和授权范围内的安全工作。工作台不会替你判断目标是否允许扫描。任何扫描、接口访问、漏洞验证或数据采集，都应由使用者确认授权范围、时间窗口和数据处理要求。

工作台本身不执行扫描器。扫描行为来自你为 Agent 配置的外部工具，相关风险和权限边界仍由使用者负责。

## 2. 分享前先清理个人数据

不要直接把自己正在使用的原目录发给别人，因为以下内容可能包含目标、资产、漏洞、路径、账号线索或内部备注：

- `info-workbench-v9\data\workbench.db`
- `info-workbench-v9\data\server.stdout.log`
- `info-workbench-v9\data\server.stderr.log`
- 导出的 JSON、HTML 报告和快照

推荐的分享方式：

1. 先双击 `停止-v9本地工作台.cmd`。
2. 完整备份自己的工作台数据。
3. 复制整个“信息收集工具集合”目录，创建一份专门用于分享的副本。
4. 只在分享副本中删除或替换 `data` 目录里的个人数据库和日志。
5. 启动分享副本，确认页面中没有个人目标和敏感结果。
6. 再将分享副本压缩后发送给朋友。

删除数据库会丢失其中的数据。务必确认操作对象是“分享副本”，不要误删自己的原目录。

## 3. 不要把本地服务暴露到公网

当前服务只监听 `127.0.0.1`，这是预期的安全边界。不要为了远程访问而直接改成 `0.0.0.0`、配置公网端口转发或关闭 Host/Origin 校验。

如果确实需要多人协作，应先增加身份认证、传输加密、权限控制、审计日志和备份策略，而不是直接开放当前本地服务。

## 4. 启动脚本为什么有两个 CMD 和两个 PS1

- `.cmd` 是适合双击的入口，负责调用 PowerShell，并在启动失败时保留报错窗口。
- `.ps1` 是实际逻辑，负责环境检查、进程管理、日志、健康检查和浏览器兜底。
- 启动和停止各有一组，所以一共是两个 CMD 和两个 PS1。

这些文件不是重复文件。分享时应全部保留，也不要单独双击 `dist/server.js` 代替启动器。

## 5. 保留完整项目结构

v9 已经不是单 HTML 文件。只发送 `index.html` 无法使用，因为页面还依赖：

- 本地 Node.js 服务；
- API 和 WebSocket；
- SQLite 数据库；
- 本地 CSS、JavaScript 和渲染资源；
- MCP 服务及其依赖。

移动项目时可以移动整个根目录，但不要随意移动、改名或删除内部的 `public`、`dist`、`data` 和依赖文件。

## 6. Node.js 与首次启动

- 项目要求 Node.js 24 或更高版本。
- 启动器找不到 `dist/server.js` 时，会执行 `npm install` 和 `npm run build`。
- 因此首次构建可能需要联网，并会比平时启动更久。
- 从不可信来源获得项目文件后，不应直接运行 `npm install`；应先检查依赖和脚本。

Windows SmartScreen 或安全软件可能提示脚本风险。请确认文件确实来自可信分享者，再选择运行；不要为了方便永久关闭系统安全功能。

## 7. 数据与备份

- SQLite 数据库是主要数据源，不要在服务运行时用第三方工具直接修改它。
- 复制数据库前，先停止工作台。
- 批量导入、重复合并、回收站清空或版本升级前，先导出完整备份。
- 浏览器快照和撤销记录用于短期恢复，不能代替离线备份。
- HTML 报告和导出文件同样可能包含敏感信息，发送前需要检查和脱敏。

首次运行时，同一浏览器中旧版 v8 的本地数据可能自动迁移到 SQLite。若要验证“干净分享包”，最好使用没有旧版数据的浏览器配置文件，或在确认备份后清理对应站点数据。

## 8. MCP 接入注意事项

- MCP 进程必须指向当前项目的绝对路径：`info-workbench-v9\dist\mcp.js`。
- MCP 只接受结构化写入，不执行 URL、命令、脚本或扫描器。
- Agent 应先调用扫描器，再解析输出，最后调用 MCP 写入。
- `operation_id` 用于防止重试造成重复写入。同一次操作重试应复用；新的操作必须更换。
- `card_key` 应在同一目标内保持稳定，否则同一类结果可能被创建成多张卡片。
- `replace` 会覆盖卡片当前内容；不确定时优先使用 `merge`，并在网页中复核历史记录。
- 单次最多写入 100 张卡片；单张卡片内容上限为 500,000 个字符。大量结果应先归纳或分批写入。
- 修改 Agent 的 MCP 配置或升级 MCP 代码后，需要重启 Agent。

不要把数据库内容、目标信息或扫描结果粘贴到不受信任的云端 Agent。使用第三方 Agent 前，应了解其数据保留和隐私政策。

## 9. 去重、删除和恢复

- 重复中心只识别高置信度规则，例如完全相同的 URL、IP、单值域名或 MCP key，并不保证业务含义一定重复。
- 合并前应检查正文、风险、状态、来源和连线。
- 普通删除和合并操作会先进入回收站，通常可以恢复。
- “清空回收站”属于永久删除，无法通过工作台撤销。
- 恢复历史版本会改变当前卡片内容，操作前建议先确认最新内容是否已经备份。

## 10. 性能和浏览器使用

- 大量卡片、长文本、代码高亮和连接线都会增加浏览器渲染负担。
- 卡顿时先将性能模式设为“开启”，并关闭暂时不用的侧栏或面板。
- 将不同阶段或不同类型的海量结果拆分到多个目标或汇总卡片中。
- 不建议在多个浏览器标签中同时高频编辑同一个工作区。
- 浏览器缓存异常时可刷新页面，但不要随意清除站点数据，旧版迁移标记和部分本地界面状态可能存放其中。

## 11. 分享前检查清单

- [ ] 使用的是分享副本，不是自己的原目录。
- [ ] 已停止本地服务。
- [ ] 已备份自己的 `workbench.db`。
- [ ] 分享副本不含个人数据库、服务日志和导出报告。
- [ ] 项目说明、使用指南和注意事项均已保留。
- [ ] 启动与停止的 CMD、PS1 文件均已保留。
- [ ] 已在另一台电脑或干净环境中测试启动。
- [ ] 已告知接收者需要 Node.js 24+。
- [ ] 已告知接收者只能在明确授权范围内使用扫描工具。

如需了解项目用途，阅读 [项目说明](./项目说明.md)；如需从启动开始操作，阅读 [使用指南](./使用指南.md)。

