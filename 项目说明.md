# 信息收集工作台 v9｜项目说明

## 1. 这是什么

信息收集工作台 v9 是一个运行在个人电脑上的本地可视化工作台，用来整理目标、资产、端口、指纹、漏洞线索、备注及它们之间的关系。

它适合安全研究、授权测试、资产梳理和调查笔记等场景。用户可以手动维护卡片，也可以让支持 MCP 的 Agent 把外部工具产生的结果自动写入卡片。

工作台本身不是扫描器，不会主动访问目标，不包含扫描器，也不会替 Agent 执行命令。完整链路是：

```text
外部扫描器或数据源
        ↓
Agent 调用、解析并归一化结果
        ↓
工作台 MCP 工具写入 SQLite
        ↓
浏览器自动同步并展示卡片、连线和扫描批次
```

## 2. 主要特点

- 纯本地运行：服务仅监听 `127.0.0.1:17321`。
- 前后端分离：浏览器负责交互，Node.js 提供本地 API、WebSocket 和静态资源。
- SQLite 持久化：`workbench.db` 是工作区的权威数据源。
- Agent/MCP 联动：即使浏览器关闭，Agent 也可以通过 MCP 向数据库写入结果。
- 可视化画布：支持卡片拖动、缩放、框选、批量操作、连线和小地图。
- 信息治理：支持搜索、筛选、风险与状态标记、重复结果检查、回收站及版本记录。
- 结果处理：Agent 写入后会形成扫描批次，可在结果收件箱中定位和标记已处理。
- 项目输出：支持导出当前目标数据、完整备份和 HTML 项目报告。
- 本地资源：Markdown 渲染和代码高亮不依赖公网 CDN。
- 大画布优化：卡片较多时可自动启用性能模式。

## 3. 项目边界

工作台负责：

- 保存和展示信息；
- 维护卡片、目标、连线、风险、状态和来源；
- 接收 Agent 已经整理好的扫描结果；
- 搜索、去重、复核、导出和备份。

工作台不负责：

- 执行扫描器或系统命令；
- 自动下载第三方工具；
- 绕过授权、访问控制或法律限制；
- 将数据上传到云端；
- 代替用户判断结果是否准确。

## 4. 运行架构

| 组件 | 作用 |
| --- | --- |
| `public/` | HTML、CSS、JavaScript 前端界面 |
| `dist/server.js` | 本地 HTTP API、WebSocket 和静态文件服务 |
| `dist/mcp.js` | 供 Agent 连接的 stdio MCP 服务 |
| `data/workbench.db` | SQLite 工作区数据库 |
| 根目录启动/停止脚本 | 检查环境、启动后台服务、打开浏览器和安全停止服务 |

浏览器和 MCP 共用同一份 SQLite 数据。服务通过 revision 和 WebSocket 通知前端刷新，因此 Agent 写入后通常不需要手动刷新页面。

## 5. MCP 工具

| 工具 | 性质 | 用途 |
| --- | --- | --- |
| `collector_list_targets` | 只读 | 查找工作台中的目标及其 `target_id` |
| `collector_get_target` | 只读 | 查看一个目标的卡片 ID 和稳定 `mcp_key` |
| `collector_create_target` | 写入 | 创建新目标 |
| `collector_upsert_cards` | 写入 | 将 Agent 整理后的结果新增或更新到卡片 |
| `collector_add_connection` | 写入 | 在两张卡片之间创建有向关系 |
| `collector_search_cards` | 只读 | 按关键词、目标、风险和标签搜索卡片 |
| `collector_list_scan_runs` | 只读 | 查看已经记录的 Agent 扫描批次 |

所有写入工具都使用 `operation_id` 保证幂等。同一次写入失败后重试应复用原 ID；新的写入任务必须换一个新 ID。

## 6. 运行要求

- Windows 10 或 Windows 11；
- Node.js 24 或更高版本；
- 支持现代 Web 标准的浏览器，推荐 Edge 或 Chrome；
- 首次缺少构建文件时，需要网络连接以执行 `npm install`；
- 保留完整目录结构，不要只复制 HTML 文件。

## 7. 目录入口

```text
信息收集工具集合/
├─ 启动-v9本地工作台.cmd      双击启动
├─ 启动-v9本地工作台.ps1      启动实现，请保留
├─ 停止-v9本地工作台.cmd      双击停止
├─ 停止-v9本地工作台.ps1      停止实现，请保留
├─ 项目说明.md
├─ 使用指南.md
├─ 注意事项.md
└─ info-workbench-v9/
   ├─ public/
   ├─ dist/
   ├─ src/
   └─ data/workbench.db
```

第一次使用请阅读 [使用指南](./使用指南.md)。转发、备份、连接 Agent 或处理敏感信息前，请阅读 [注意事项](./注意事项.md)。

