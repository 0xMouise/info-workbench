# 信息收集工作台 v9｜使用指南

## 1. 快速启动

1. 将整个“信息收集工具集合”目录复制或解压到本地，保留原有目录结构。
2. 确认电脑安装了 Node.js 24 或更高版本。在终端执行 `node --version` 可以查看版本。
3. 双击根目录的 `启动-v9本地工作台.cmd`。
4. 启动器会依次检查环境、服务和数据库，然后打开默认浏览器。
5. 浏览器地址为 <http://127.0.0.1:17321/>。
6. 使用结束后，双击 `停止-v9本地工作台.cmd`。

启动窗口自动关闭不代表服务停止；服务会继续在后台运行。再次双击启动脚本时，如果服务已经运行，启动器只会打开页面，不会重复启动。

如果浏览器没有自动打开，可以手动访问 <http://127.0.0.1:17321/>。如果启动失败，窗口会保留错误信息；请先阅读提示，不要立即关闭窗口。

## 2. 第一次使用

### 创建和切换目标

- 新建目标后，在顶部目标标签之间切换工作区。
- 双击目标标签可以重命名。
- 删除目标时会先移入回收站，确认无误后再清空回收站。

### 添加和编辑卡片

- 在“常用组合”中可一键创建 `SRC 信息收集专项` 或 `EDUSRC 专项`，每组包含 12 张按工作阶段排列的结构化卡片。
- SRC 专项覆盖授权边界、资产归属、域名/DNS、子域、网络服务、技术栈、Web/API、移动端、供应链和优先级交接。
- EDUSRC 专项覆盖学校边界、教育资产、系统入口、授权测试身份、统一认证、接口面、越权矩阵、敏感数据、专项验证、跨端链路和提交证据。
- 专项卡片自带 Markdown 表格和检查项；其中的个人标识与响应证据应始终脱敏，并只保留证明问题所需的最少数据。
- 从卡片模板中选择合适类型，例如子域名、端口、指纹、漏洞或备注。
- 填写标题和内容后，将卡片拖到合适位置。
- 卡片内容支持 Markdown；代码内容可以高亮展示。
- 使用风险等级、处理状态和标签对结果分类。
- 卡片菜单可查看来源和历史版本；Agent 覆盖或人工编辑前会保留可恢复记录。

### 操作画布

- 在空白处拖动：平移画布。
- `Ctrl + 鼠标滚轮`：以鼠标所在位置为中心缩放。
- 拖动卡片：调整位置。
- 拖动卡片边缘：调整大小。
- 框选或多选卡片：执行批量移动、连接或删除。
- 选择两张卡片后：创建它们之间的关系线。
- `Home`：让视图重新适配当前卡片范围。

连接线会跟随卡片位置和画布缩放更新。画布较大时，可用小地图快速定位。

## 3. 搜索、复核与整理

- 搜索：按标题或内容查找卡片，也可结合筛选条件缩小范围。
- 项目概览：查看风险分布、处理进度、Agent 结果数、重复组和来源工具覆盖。
- 重复结果：检查相同完整 URL、IP、单值域名或 MCP key。合并前请人工确认；被合并的卡片会先进入回收站。
- Agent 结果收件箱：按扫描批次查看新增结果，定位到对应卡片，并标记为已处理。
- 历史版本：查看卡片来源、工具、批次和修改记录，必要时恢复旧版本。
- 回收站：恢复误删的目标或卡片；清空回收站属于永久删除。

## 4. 常用快捷键

| 快捷键 | 功能 |
| --- | --- |
| `Ctrl + K` | 打开命令面板 |
| `Ctrl + F` | 打开搜索 |
| `Ctrl + S` | 导出当前目标数据 |
| `Ctrl + Z` | 撤销 |
| `Ctrl + Y` 或 `Ctrl + Shift + Z` | 重做 |
| `Ctrl + A` | 全选当前目标的卡片 |
| `Delete` | 删除已选卡片并移入回收站 |
| `Ctrl + +` / `Ctrl + -` | 放大 / 缩小画布 |
| `Ctrl + 鼠标滚轮` | 围绕鼠标位置缩放 |
| `Home` | 适配并回到卡片区域 |
| `Esc` | 清除选择或关闭当前面板 |

在输入框或卡片编辑区内，系统会尽量保留文本编辑所需的原生全选、撤销和删除行为；`Ctrl + S` 与 `Ctrl + F` 仍属于工作台快捷键。

## 5. 性能模式

工作台默认使用“自动”性能模式。卡片较多时，它会减少视口外元素和部分视觉效果的渲染，以提升拖动画布和缩放时的流畅度。

可通过顶部性能按钮或 `Ctrl + K` 命令面板切换：

- 自动：按画布规模判断；
- 开启：始终使用性能优化；
- 关闭：保留全部视觉效果。

如果大量卡片下仍感觉掉帧，优先开启性能模式，并将无关结果拆分到不同目标或卡片中。

## 6. 导出、备份和恢复

- `Ctrl + S`：导出当前目标，适合单独分享一个目标的数据。
- 项目数据菜单：导入、导出或管理工作台数据。
- 导出项目报告：生成便于阅读和交付的 HTML 报告。
- 完整备份：在升级、迁移、批量合并或清理数据前使用。
- 最近快照：适合恢复近期误操作，但不能替代独立备份文件。

数据库默认位于：

```text
info-workbench-v9\data\workbench.db
```

复制数据库备份前，建议先用停止脚本关闭服务，避免复制到正在写入的中间状态。

## 7. 连接支持 MCP 的 Agent

工作台通过 stdio MCP 与 Agent 联动。不同 Agent 的配置文件位置和字段可能不同，但核心配置都是让 Agent 启动本项目的 `dist/mcp.js`。

通用配置示例：

```json
{
  "mcpServers": {
    "info-workbench-v9": {
      "command": "node",
      "args": [
        "C:\\Users\\你的用户名\\Desktop\\信息收集工具集合\\info-workbench-v9\\dist\\mcp.js"
      ]
    }
  }
}
```

如果 Agent 找不到 `node`，将 `command` 改成 `node.exe` 的绝对路径，例如：

```json
"command": "C:\\Program Files\\nodejs\\node.exe"
```

配置后重启 Agent。工作台网页可以关闭，MCP 仍会直接读写同一 SQLite 数据库；重新打开网页后会显示最新结果。

### 推荐的 Agent 写入流程

1. Agent 调用自己已经配置好的扫描器或数据源。
2. Agent 解析输出，去除无关日志，并整理成标题、正文、风险、状态和标签。
3. 调用 `collector_list_targets` 查找目标；不存在时调用 `collector_create_target`。
4. 需要判断是否更新已有卡片时，调用 `collector_get_target` 或 `collector_search_cards`。
5. 调用 `collector_upsert_cards` 写入结果。
6. 存在明确关系时，调用 `collector_add_connection` 创建连线。
7. 在网页的 Agent 结果收件箱中人工复核。

可以直接给 Agent 这样的任务说明：

> 在我明确授权的范围内，调用你已有的扫描工具完成信息收集。解析并归一化结果后，通过 info-workbench-v9 MCP 写入指定目标。先查询目标和已有卡片，再调用 collector_upsert_cards。每个新写入任务使用新的 operation_id；只有重试同一次写入时才复用原 operation_id。不要让工作台执行扫描器。

### 写入卡片时的重要字段

- `target_id`：结果要进入的目标 ID。
- `operation_id`：长度 8～160，只能使用字母、数字、点、下划线、冒号和短横线。
- `source.agent`：Agent 名称。
- `source.tool`：真正产生结果的扫描器或数据源名称。
- `card_key`：目标内稳定的卡片键。复用它可更新同一张卡片。
- `write_mode`：`merge` 去重合并、`append` 追加、`replace` 覆盖、`new` 始终新建。
- `risk`：`critical`、`high`、`medium`、`low` 或 `info`。
- `status`：`todo`、`doing` 或 `done`。

## 8. 启动故障排查

| 现象 | 处理方法 |
| --- | --- |
| 提示未找到 Node.js | 安装 Node.js 24+，关闭并重新打开启动窗口 |
| 浏览器没有打开 | 手动访问 `http://127.0.0.1:17321/` |
| 提示端口 17321 被占用 | 先双击停止脚本；仍存在时检查是否有其他程序占用该端口 |
| 首次构建失败 | 检查网络、npm 配置和磁盘空间，再重新启动 |
| 页面能开但无法保存 | 查看 `info-workbench-v9\data\server.stderr.log` |
| Agent 看不到 MCP 工具 | 检查 `dist/mcp.js` 的绝对路径和 Node 路径，然后重启 Agent |
| Agent 已写入但页面没更新 | 查看页面数据库连接状态；必要时刷新页面或重启本地工作台 |

更重要的安全和分享事项请继续阅读 [注意事项](./注意事项.md)。
